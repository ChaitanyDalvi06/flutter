import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SpendWise app smoke test', (WidgetTester tester) async {
    // Placeholder test - full integration tests require DB setup
    expect(true, isTrue);
  });
}
