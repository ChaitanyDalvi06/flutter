/// Central configuration for the SpendWise app.
/// Change [baseUrl] to match where your backend server is running.
class AppConfig {
  static const String baseUrl = 'https://spendwise-mobile-app.onrender.com/api';
}
